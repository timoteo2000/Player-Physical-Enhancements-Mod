package net.timoteo2000.ppem.client.settings;

import org.lwjgl.input.Keyboard;

import net.minecraft.client.settings.KeyBinding;
import net.timoteo2000.ppem.reference.Names;

public class KeyBindings {
	/*public static KeyBinding open = new KeyBinding(Names.Keys.OPEN, Keyboard.KEY_O, Names.Keys.CATEGORY);*/ //This define the "default" key binding for whatever key binding is set here, as well as the category.
}